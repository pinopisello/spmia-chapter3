spring:
  application:
    name: licensingservice
  profiles:
    active:
      default
  cloud:
    config:
      uri: http://localhost:8888
      enabled: true  #Abilita properties retrieval in ConfigServicePropertySourceLocator